{
  "name": "usafitness-landings",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "node dist/server/entry.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:ci": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@astrojs/node": "^10.0.4",
    "astro": "^6.1.5"
  }
}
